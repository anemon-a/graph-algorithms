#include "s21_graph_algorithms.h"

std::vector<int> GraphAlgorithms::DepthFirstSearch(IGraph &graph, int start_vertex)
{
  return std::vector<int>();
}

std::vector<int> GraphAlgorithms::BreadthFirstSearch(IGraph &graph, int start_vertex)
{
  return std::vector<int>();
}
